import { h } from "preact";

export default function NotFoundPage() {
  return (
    <main class="page text-center h-full">
      <span class="mt-32 block text-5xl">Page not found</span>
    </main>
  );
}
