export enum KeyEncoding {
  Utf8 = "utf-8",
}

export enum ValueEncoding {
  Binary = "binary",
  Json = "json",
  Utf8 = "utf-8",
}

export enum DbChangeType {
  Create = "create",
  Update = "update",
  Delete = "delete",
}